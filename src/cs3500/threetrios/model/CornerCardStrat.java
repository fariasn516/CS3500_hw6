package cs3500.threetrios.model;

/**
 * A strategy that prioritizes placing the best cards in corner positions.
 */
public class CornerCardStrat implements GameStrategy {
  @Override
  public GameMove chooseMove(Player player, Grid grid) {
    // Logic to choose a move that places a card in a corner
    // Return a Move object representing the chosen move
    return null;
  }
}
